import { NestInterceptor, ExecutionContext, HttpService, CallHandler } from "@nestjs/common";
import { Observable } from "rxjs";
import { LoggerService } from "./logger.service";
export declare class LoggerInterceptor implements NestInterceptor {
    private readonly logger;
    private readonly httpService;
    constructor(logger: LoggerService, httpService: HttpService);
    intercept(context: ExecutionContext, next: CallHandler): Observable<any>;
}
